import 'package:flutter/material.dart';

class EndGamePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Center(
          child: Text('END GAME TODO'),
        ),
      ),
    );
  }
}
